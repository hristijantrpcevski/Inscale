const initialState = {
  books: [],
  loading: true,
};

const usersReducer = (state = initialState, action: any) => {
  switch (action.type) {
    default:
      break;
  }
  return state;
};

export default usersReducer;
